{
    "_comment": "Creating ansble admin user copying ssh key",
    "variables": {
        "region":  "",
        "source_ami": "",
        "instance_type": "",
        "vpc_id": "",
        "subnet_id": ""
    },
    "_comment1": "Packer build -var \"aws_secret_key=foo\" packer.json",
    "_comment2": "Packer build -var-file packer-vars.json packer.json",
    "builders": [
        {
            "type": "amazon-ebs",
            "region": "{{user `region`}}",
            "source_ami": "{{user `source_ami`}}",
            "instance_type": "{{user `instance_type`}}",
            "ssh_username": "ubuntu",
            "ami_name": "DevOpsClass-B26-ansible2-{{isotime | clean_resource_name}}",
            "vpc_id": "{{user `vpc_id`}}",
            "subnet_id": "{{user `subnet_id`}}",
            "tags": {
                "Name": "HSK-{{isotime | clean_resource_name}}"
            }
        }
    ],
    "provisioners": [
        {
            "type": "shell",
            "inline": [
                "sudo useradd -m ansbleadmin --shell /bin/bash",
                "sudo mkdir -p /home/ansibleadmin/.ssh",
                "sudo chown -R ansibleadmin /home/ansibleadmin/",
                "sudo touch /home/ansibleadmin/.ssh/authorized_keys",
                "sudo usermod -aG sudo ansbleadmin",
                "echo 'ansibleadmin ALL=(ALL) NOPASSWD: ALL' | sudo tee -a /etc/sudoers",
                "echo '' | sudo tee /home/ansibleadmin/.ssh/authorized_keys"
            ]
        }
    ]
}